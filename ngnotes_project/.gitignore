# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Environment
.env
.env.local

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc

# OS
.DS_Store
Thumbs.db
desktop.ini

# MS Office lock files (e.g. "~$nthly Report Template.docx")
~$*

# Locally-built launcher executables (built via launcher/build.py) -- not
# checked in, same reasoning as node_modules/.venv: build artifacts, not
# source. NGNotes.app is a directory (a macOS app bundle), not a single file.
/NGNotes
/NGNotes.exe
/NGNotes.app/
launcher/_build/
launcher/*.spec
